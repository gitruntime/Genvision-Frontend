import { CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";

const AssignmentSubmission = () => {
  return (
    <CardContent>
      <Input disabled={true} name="Assignment" />
      <Input disabled={true} name="Assignment" />
      <Input disabled={true} name="Assignment" />
    </CardContent>
  );
};

export default AssignmentSubmission;
